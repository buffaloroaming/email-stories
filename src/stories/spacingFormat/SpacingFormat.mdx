import { Canvas, Meta, Unstyled, Controls, Primary } from '@storybook/blocks'
import { spacingFormatCode } from './spacingFormatCode'
import { gitHubAction } from '../actions/actions'
import * as Spacing from './SpacingFormat.stories'

<Meta of={Spacing} />

<Unstyled>
  <section style={{ maxWidth: '640px' }}>
    <h1>Spacing</h1>

      Intended spacing between each module is 40px and spacing between module elements is 20px. Of
      course, more space can be added if needed, just follow the pattern.
    	<div style={{ height: '20px' }}></div>
      <span style={{fontWeight: 700}}>Spacing top/bottom of each module: </span> <span style={{ color: '#e97c3d',fontWeight: 700 }}>40px</span>
    	<br />

      <span style={{fontWeight: 700}}>Spacing between elements within a module: </span> <span style={{ color: '#e97c3d',fontWeight: 700 }}>20px</span>

  <div style={{ height: '20px' }}></div>
  </section>
</Unstyled>
<section style={{ maxWidth: '1280px' }}>
  <Primary of={Spacing} additionalActions={[gitHubAction]}
      source={{ code: spacingFormatCode }} />
  <Controls />

  <div style={{ height: '10px' }}></div>
  <Canvas of={Spacing.Spacing10} additionalActions={[gitHubAction]}
      source={{ code: spacingFormatCode }} />
  <div style={{ height: '10px' }}></div>
  <Canvas of={Spacing.Spacing20} additionalActions={[gitHubAction]}
      source={{ code: spacingFormatCode }} />
  <div style={{ height: '10px' }}></div>
  <Canvas of={Spacing.Spacing30} additionalActions={[gitHubAction]}
      source={{ code: spacingFormatCode }} />
  <div style={{ height: '10px' }}></div>
  <Canvas of={Spacing.Spacing40} additionalActions={[gitHubAction]}
      source={{ code: spacingFormatCode }} />
</section>
