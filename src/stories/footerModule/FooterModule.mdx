import { Canvas, Meta, Unstyled, Story, Primary, Controls } from '@storybook/blocks'

import * as FooterStories from './FooterModule.stories'

import { FooterEnglishCode } from './FooterEnglishCode.js'
import { FooterSpanishCode } from './FooterSpanishCode.js'
import { gitHubAction } from '../actions/actions.js'

import '@/assets/styles/main.css'

<Meta of={FooterStories} />

<Unstyled>
  <section>
    <h1>footers</h1>
    <p>
      The Footer is built with variants including language (English and Spanish) and legal copy.
    </p>
    <Primary of={FooterStories} />
    <Controls />
    <h2>English</h2>
    <Canvas
      of={FooterStories.English}
      additionalActions={[gitHubAction]}
      source={{ code: FooterEnglishCode }}
    />
    <h2>Spanish</h2>
    <Canvas
      of={FooterStories.Spanish}
      additionalActions={[gitHubAction]}
      source={{ code: FooterSpanishCode }}
    />
  </section>
</Unstyled>
./FooterModule.stories
